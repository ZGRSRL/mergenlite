# =============================================================================
# MergenLite — Environment Configuration (TEMPLATE)
# =============================================================================
# Copy this file to .env and fill in your values.
#   cp .env.example .env
#   cp .env.example mergen/.env
# =============================================================================

# ---- API Keys ----
OPENAI_API_KEY=sk-your-openai-key-here
SAM_API_KEY=SAM-your-sam-api-key-here
AMADEUS_API_KEY=your-amadeus-key
AMADEUS_API_SECRET=your-amadeus-secret
AMADEUS_ENV=production

# ---- Database ----
# Local dev uses localhost; Docker Compose overrides with 'db' hostname
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=mergenlite
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme

# DATABASE_URL overrides individual vars (Docker Compose sets this)
# DATABASE_URL=postgresql://postgres:changeme@localhost:5432/mergenlite

# ---- Email / SMTP ----
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_USE_TLS=true
SMTP_FROM_EMAIL=noreply@mergenlite.com
PIPELINE_NOTIFICATION_EMAIL=

# ---- Auth (Simple HTTP Basic) ----
# Format: username:password
AUTH_USER_1=admin:changeme
AUTH_USER_2=viewer:changeme

# ---- Application ----
PORT=8000
ENV=dev
LOG_LEVEL=INFO
VITE_API_URL=http://localhost:8000
